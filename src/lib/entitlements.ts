// Testing bypass: payment enforcement is OFF so the product can be tested
// end-to-end without an active subscription.
// To re-enable payments, set PAYMENTS_ENFORCED = true.
export const PAYMENTS_ENFORCED = false;
